# Ghost CMS Environment Configuration
# Copy this file to .env and update with your actual values
# SECURITY: Never commit .env to version control!

# ============================================
# Database Passwords
# ============================================
# IMPORTANT: Generate strong passwords (32+ characters)
# Use: openssl rand -base64 32 | tr -d "=+/" | cut -c1-32
GHOST_DB_PASSWORD=change_this_to_strong_password_min_32_chars
MYSQL_ROOT_PASSWORD=change_this_to_different_strong_password_min_32_chars

# ============================================
# Cloudflare Tunnel Configuration
# ============================================
# Get your token from: https://one.dash.cloudflare.com/
# Navigate to: Networks → Tunnels → Create/Edit Tunnel
CLOUDFLARE_TUNNEL_TOKEN=your_cloudflare_tunnel_token_here

# ============================================
# Mail Configuration (Optional but recommended)
# ============================================
# Ghost needs email for password resets, member invitations, etc.
# Configure one of the supported providers below:

# Mailgun (Recommended)
MAIL_HOST=smtp.mailgun.org
MAIL_PORT=587
MAIL_USER=postmaster@mg.yourdomain.com
MAIL_PASSWORD=your_mailgun_password_here
MAIL_FROM=noreply@yourdomain.com

# Gmail (requires app-specific password)
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USER=your.email@gmail.com
# MAIL_PASSWORD=your_app_specific_password
# MAIL_FROM=your.email@gmail.com

# SendGrid
# MAIL_HOST=smtp.sendgrid.net
# MAIL_PORT=587
# MAIL_USER=apikey
# MAIL_PASSWORD=your_sendgrid_api_key
# MAIL_FROM=noreply@yourdomain.com

# ============================================
# Backup Encryption (Optional but recommended)
# ============================================
# Encrypt backups for enhanced security
# Generate with: openssl rand -base64 32
# BACKUP_ENCRYPTION_KEY=your_strong_encryption_passphrase_here
